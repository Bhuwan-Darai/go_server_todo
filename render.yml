services:
  - type: web
    name: github.com/Bhuwan-Darai/goCrud
    env: docker
    dockerfilePath: ./Dockerfile.render
    region: oregon
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 8080
