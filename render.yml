services:
  - type: web
    name: github.com/Bhuwan-Darai/goCrud
    env: docker
    region: oregon
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 8080
